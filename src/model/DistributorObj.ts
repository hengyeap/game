import 'reflect-metadata';
import { JsonProperty } from "../json/JsonProperty";
export default class DistributorObj {
  avatar?: string = undefined;// 用户头像
  code?: string = undefined; // 用户code
  levelName?: string = undefined; // 用户等级名
  realName?: string = undefined; // 名字
  familyName?: string = undefined; // 家族名
}
